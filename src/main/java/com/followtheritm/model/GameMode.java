package com.followtheritm.model;

public enum GameMode {
    /** Classic: sequence grows by one colour each round. */
    CLASSIC,
    /** Random: a brand-new sequence is generated every round. */
    RANDOM
}
